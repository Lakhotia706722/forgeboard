# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0

# Pydantic / settings
pydantic==2.7.1
pydantic-settings==2.2.1

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
passlib==1.7.4

# Celery / Redis
celery==5.4.0
redis==5.0.4
croniter==2.0.5

# Anthropic
anthropic==0.25.9

# Encryption (MVP-grade — note: upgrade to HashiCorp Vault / AWS Secrets Manager before production)
cryptography==42.0.7

# Google OAuth / API clients
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.127.0

# HTTP client
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.9

# ── Phase 8: Voice & Telephony ────────────────────────────────────────────────
# Twilio (primary telephony vendor — behind abstraction interface)
twilio==9.2.3
# Deepgram STT (behind abstraction interface)
deepgram-sdk==3.2.7
# ElevenLabs TTS (behind abstraction interface)
elevenlabs==1.3.0
# WebSocket support for real-time audio streaming
websockets==12.0
